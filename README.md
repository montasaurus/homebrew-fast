## Installation

To get the `codex` update faster:

```bash
brew uninstall codex # remove the previous version
brew tap montasaurus/fast # tap this repo
brew install montasaurus/fast/codex
```

## Notes

I created this so I didn't have to wait hours for the Codex updates to work through the Homebrew release pipeline. I'll assume it's just me using it unless people star it!
